name: Integration Tests

on:
    workflow_dispatch:
    pull_request:
        paths:
            - '**/integration-tests*'
            - '**/docker/**'
            - '**.php'
            - 'phpunit*'
    push:
        paths:
            - '**/integration-tests*'
            - '**/docker/**'
            - '**.php'
            - 'phpunit*'

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    integration-tests:
        runs-on: ubuntu-latest
        if: ${{ !contains(github.event.head_commit.message, 'integration-tests skip') }}
        strategy:
            matrix:
                php-version: [ '7.2', '7.3', '7.4' ]
                wp-version: [ '5.8', '5.9', '6.0' ]
        steps:
            -   name: Checkout
                uses: actions/checkout@v3

            -   name: Setup PHP
                uses: shivammathur/setup-php@v2
                with:
                    php-version: ${{ matrix.php-version }}
                    coverage: none

            -   name: Adjust Dependencies
                run: |
                    composer config --unset repositories.0
                    composer remove inpsyde/php-coding-standards inpsyde/wp-stubs vimeo/psalm roots/wordpress --dev --no-update
                    composer require roots/wordpress:${{ matrix.wp-version }} --no-update

            -   name: Install Dependencies
                uses: ramsey/composer-install@v2

            -   name: Run integration tests (PHP ${{ matrix.php-version }}, WP ${{ matrix.wp-version }})
                run: |
                    chmod u+x ./tests/integration-tests.sh
                    ./tests/integration-tests.sh ${{ matrix.php-version }}
